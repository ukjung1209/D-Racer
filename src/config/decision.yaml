# 대회 주행용 파라미터 (race.launch.py가 읽음).
# ── 주행마다 여기 숫자만 고친다. 파이썬 런치는 안 건드림. ──
# 실시간 튜닝은 그대로: ros2 param set /decision_node steer_kp 1.2
#
# 트랙/조명이 크게 바뀌면 이 파일을 복사해 decision_track_A.yaml 식으로 프리셋을
# 만들고  ros2 launch inference race.launch.py params_file:=.../decision_track_A.yaml
# 로 골라 실행하면 된다.

# =====================================================================
#  통합 결정 노드 (차선추종 PD + 신호등 + 갈림길 + 동적장애물)
# =====================================================================
decision_node:
  ros__parameters:
    # --- 차선추종 PD (주행마다 제일 자주 바뀌는 값) ---
    steer_kp: 0.8
    steer_kd: 0.45
    steer_ka: 0.0
    steering_sign: -1.0        # 반대로 꺾이면 1.0
    base_throttle: 0.31
    lane_timeout_sec: 0.5

    # --- 가변속도 (직선 빠르게 / 코너 미리 감속) ---
    speed_ka: 0.4               # |angle|(전방 곡률) 예측 감속. ↑=코너서 더 느리게
    speed_ko: 0.4               # |offset|(현재 이탈) 보정 감속
    min_throttle: 0.10          # 감속 하한 (이보다 안 느려짐)
    throttle_accel_rate: 0.5    # 가속 슬루(초당 상한). ↓=더 완만하게 가속(튐 방지)

    # --- 미션 on/off (튜닝 땐 한 미션만 켜고 나머지 끔) ---
    enable_light_mission: true
    enable_fork_mission: true
    enable_obstacle_mission: true

    # --- 신호등 (초록불 출발 / 빨간불 정지) ---
    light_conf_min: 0.35        # object_node conf_threshold(0.35)와 맞춤: 박스 보이면 출발
    green_votes_needed: 1       # 초록불 1프레임만 봐도 출발
    red_votes_needed_light: 2   # 빨간불 2프레임 연속이면 정지

    # --- 좌/우 갈림길 (표지판 보이면 즉시 그 라인 hugging + 1회 stop-and-go) ---
    sign_conf_min: 0.5
    sign_area_min: 0.01         # 이 크기 이상이어야 방향 인정 (너무 일찍 틀면 ↑)
    hug_hold_sec: 1.0           # 표지판 놓치고 이 시간 뒤 평소 복귀
    sign_stop_sec: 0.7          # 표지판 앞 정지 시간(stop-and-go), 0=끔
    stop_lane_conf_min: 0.5     # 두 차선 이 conf 이상일 때만 정지

    # --- 동적 장애물 (빨강 감속 → 아루코 정지 → 통과 후 재출발) ---
    slow_factor: 0.9            # 빨강 구간 감속 = 원속도의 90%
    red_roi_top_frac: 0.5
    red_ratio_trigger: 0.15
    red_votes_needed_obstacle: 3
    red_clear_time_sec: 1.0
    # 아루코 (이 마커 = 6X6_50 / ID3. 320x160 저해상도 실측 튜닝값)
    aruco_dict: '6X6_50'
    aruco_target_id: 3          # 이 ID만 정지(-1=아무 ID나)
    aruco_upscale: 2            # 불안정하면 3
    aruco_clahe: false          # 조명 나쁘면 true
    aruco_votes_needed: 1       # 보이면 바로 정지
    clear_time_sec: 1.5
    aruco_min_perimeter_rate: 0.01
    aruco_poly_accuracy: 0.08
    aruco_adaptive_win_max: 45
    aruco_error_correction: 0.4

# =====================================================================
#  차선 검출 (BEV 사다리꼴/흰색 마스크는 트랙마다 바뀜)
# =====================================================================
lane_node:
  ros__parameters:
    lane_color: 'white'
    white_s_max: 20             # 채도 상한↓ = 색깔 배제. 40→20: 푸르스름한 트랙 무늬(반사) 오검출 배제
    white_v_min: 120            # 명도 하한 절대 바닥 (흰선 없는 프레임에서 매트 안 잡히게)
    white_v_percentile: 98.0    # (mod 5) V 하한을 프레임 상위 이 백분위로 적응 조정 (밝은 매트 배제). ↓=더 관대
    white_tophat_ksize: 21      # 글레어 남으면 ↓, 차선 끊기면 ↑ (0=끔)
    white_tophat_min: 18
    roi_top_px: 45
    num_bands: 10
    line_split_gap_px: 40
    lane_half_width_px: 90
    # --- 클러스터 검증(mod 1): 십자 가로획/잡음 조각을 배정 전에 걸러냄 ---
    cluster_max_width_px: 60    # BEV 차선폭 ~2.5배. 넘으면 가로획으로 보고 버림 (십자 무늬 차단)
    cluster_min_pixels: 30      # 실제 흰 픽셀 수 하한 (잡음 조각 배제)
    anchor_gate_ratio: 0.4      # |cx−앵커| > half_width·이 값 이면 배정 안 함 (오배정 차단)
    # --- 앵커 갱신 EMA(mod 2): 밴드 간은 크게(커브 추종) / 프레임 간은 작게(안정) ---
    anchor_alpha_band: 0.6      # 밴드 간 EMA 계수 (↑=커브 더 빨리 따라감)
    anchor_max_step_band_px: 25 # 밴드 간 이동량 상한(px) — 한 스텝에 이보다 못 튐
    anchor_alpha_frame: 0.35    # 프레임 간 EMA 계수 (↓=프레임 튐에 더 둔감)
    anchor_max_step_frame_px: 15 # 프레임 간 이동량 상한(px)
    flip_confirm_frames: 3      # 좌/우 반전을 확정하는 연속 프레임 수 (↑=더 끈질기게 유지)
    # --- 재획득(mod 4): 앵커 잃은 뒤 좌/우 다시 잡기 ---
    hist_width_tol: 0.25        # 히스토그램 두 피크 간격 허용오차(차선폭 대비). ↑=관대
    reacquire_margin: 0.3       # 한쪽 라인 양가설 점수 차 하한 (미만이면 판정 보류)
    hug_bias_px: 90             # 갈림길 hugging 시 라인에서 떨어지는 정도(BEV px)
    # --- BEV 사다리꼴 (트랙 실측 튜닝값) ---
    bev_top_left: 0.1
    bev_top_right: 0.9
    bev_top_y: 0.32
    bev_bottom_left: 0.0
    bev_bottom_right: 1.0
    publish_debug: true

# =====================================================================
#  물체 검출 (YOLO: left/right 표지판 + green/red 신호등)
# =====================================================================
object_node:
  ros__parameters:
    conf_threshold: 0.35
    yolo_gamma: 0.4             # 노출 낮아 어두우면 ↓
    yolo_clahe: false
    publish_debug: true
